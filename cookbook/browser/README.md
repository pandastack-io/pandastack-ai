# browser — Headless Chromium via Playwright

Pre-installed: Chromium + Playwright with the browser deps. Use for
scraping, screenshotting, or running e2e tests inside an isolated VM.

```bash
python main.py
```
