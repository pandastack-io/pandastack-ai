# code-interpreter — Python with the PyData stack

Pre-installed: `python3`, `numpy`, `pandas`, `matplotlib`, `scipy`,
`scikit-learn`. The default template for ML/data workloads.

```bash
python main.py
```

For headless web extraction and scraping see [`../browser`](../browser/) — it
reuses the same sandbox across multiple `sb.exec()` calls.
